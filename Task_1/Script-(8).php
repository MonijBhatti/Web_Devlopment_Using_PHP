<!-- Script_8: Use the echo statement to display the length of a given string stored in the variable $text. -->

<?php
// Define the string
$text = "Hello, World!";

// Find the length of the string
$length = strlen($text);

// Display the length using echo
echo "The length of the string is: $length";
?>
